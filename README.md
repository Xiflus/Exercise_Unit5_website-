# Exercise_Unit5_website-
Práctica entornos 1ºDAM semipresencial
En este repositorio voy a trabajar la práctida de entornos de 1ºDAM semipresencial.
Las tecnologias a utilizar van a ser HTML y CSS.
Para iniciar, lanzar index.html en un navegador
$ git branch
$ git switch main
$ git merge about
$ git push
$ git branch home
$ git switch home
$ git merge home
$ git status
$ git add .
$ git status
$ git commit
$ git push
$ git log --oneline --graph --all
$ git tag v1-0 99b39bb
$ git log --oneline --graph --all
$ git tag v1.1 8c4100e
$ git push
